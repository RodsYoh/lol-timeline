import styled from "@emotion/styled";

export const CheckboxContainer = styled.label`
  display: flex;
  align-items: center;

  svg {
    margin-right: 0.5rem;
  }

  input {
    opacity: 0;
    visibility: hidden;
  }
`;
